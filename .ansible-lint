# The linter picks up a legacy role, and the only way to move the overall fix
# forward is to accept we're not going to fix this in older stable branches.
warn_list:  # or 'skip_list' to silence them completely
  - no-changed-when  # Commands should not change things if nothing needs doing
